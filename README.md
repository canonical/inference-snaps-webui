# Inference snaps UI

Clone the CLI repo:
```
git clone https://github.com/canonical/inference-snaps-cli
cd inference-snaps-cli
```

Run the debug command:
```
go run ./cmd/cli debug serve-ui ../static
```

Set `-h` flag for more options.
